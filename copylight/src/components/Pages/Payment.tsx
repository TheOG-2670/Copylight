import React from "react";
import AppAppBar from "../../views/AppAppBar";

const Payment = () => {
    return (
        <React.Fragment>
            <AppAppBar />
        </React.Fragment>
    );
};

export default Payment;