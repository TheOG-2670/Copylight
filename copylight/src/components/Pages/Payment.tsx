import React from "react";
import Checkout from "../Checkout";

const Payment = () => {
    return (
        <React.Fragment>
            <Checkout/>
        </React.Fragment>
    );
};

export default Payment;